<h1 class="contact">お問い合わせフォーム</h1>

<%= form_for(@contact) do |f| %>


       <h4>タイトル</h4>
		<%= f.text_field :contact_title %>
	   <h4>内容</h4>
		<%= f.text_area :contact_text %>

		   <%= f.submit '送信' %>


<% end %>